import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    
}

export const todoSlice = createSlice({
    name: "todos",
    initialState,
    reducers: {
        prefillData(state, action){

        }
    }
})